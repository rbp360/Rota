# Exclude large data and tool files
*.xlsx
*.db
*.ics
*.txt
!requirements.txt
!runtime.txt
*.md
*.pyc
__pycache__/

# Archive
data_archive/

# Exclude root-level Python tools NOT needed for production
# But we must NOT ignore the folders Vite needs for building
/*.py
!/api/
!/backend/

# Folders to IGNORE in the final bundle
.git/
.vercel/
.firebase/
node_modules/
frontend/node_modules/

# We must NOT ignore frontend/src/ or frontend/public/ in .vercelignore
# because Vite needs them to build the static site!
# We will use vercel.json to exclude them from the API function instead.
