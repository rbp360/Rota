{
    "name": "rota-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
        "build": "cd frontend && npm install && node ../node_modules/vite/bin/vite.js build --emptyOutDir"
    },
    "devDependencies": {
        "vite": "^7.3.1",
        "@vitejs/plugin-react": "^4.2.0"
    }
}